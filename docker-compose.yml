version: '3.8'

services:
  openphonebook:
    build: .
    container_name: openphonebook
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
